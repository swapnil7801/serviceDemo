import { DirectionsMapDirectiveDirective } from './directions-map-directive.directive';

describe('DirectionsMapDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectionsMapDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
